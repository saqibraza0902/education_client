"use client";
import Common from "@/layout/CommonLayout";
import PurchasedCourses from "@/layout/PurchasedCourses";
import React from "react";

const Courses = () => {
  return (
    <Common>
      <PurchasedCourses />
    </Common>
  );
};

export default Courses;
